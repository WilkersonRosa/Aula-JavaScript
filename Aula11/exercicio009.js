function verificar(){
    var pais = window.document.getElementById("meupais")
    var textloc = window.document.getElementById("paistxt")

    tex.innerHTML = `Sua velocidade atual é de ${vel}km/h.`
    if (pais == 'Brasil'){
        textloc.innerHTML
    }
    else{
        console.log(`Você é gringo.`)
    } 
}

